#include <bits/stdc++.h>

constexpr auto MAXN = 1000001;
int r[MAXN];

int
main()
{
    int n, m;
    int d, s, t;

    std::cin >> n >> m;

    for (int i = 1; i <= n; i++) {
        std::cin >> r[i];
    }

    return 0;
}